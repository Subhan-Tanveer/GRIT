// Deployed backend on Render — reachable from anywhere, no local network or
// running terminal required.

class SocialConfig {
  static const String apiBaseUrl = 'https://grit-backend-kl36.onrender.com/api';
}
