// Dev-only: your PC's LAN IP, so your phone can reach the backend over WiFi.
// Update this if your IP changes (run `ipconfig` and look for the WiFi
// adapter's IPv4 address), or swap for a real deployed URL later.

class SocialConfig {
  static const String apiBaseUrl = 'http://192.168.10.10:4000/api';
}
