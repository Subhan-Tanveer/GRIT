// Copy this file to ai_config.dart (same folder) and fill in your real key.
// ai_config.dart is git-ignored — it will never be committed.

class AiConfig {
  static const String nvidiaApiKey = '';
}
